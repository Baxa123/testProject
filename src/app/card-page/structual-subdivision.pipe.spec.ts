import { StructuralSubdivisionPipe } from './structual-subdivision.pipe';

describe('StructualSubdivisionPipe', () => {
  it('create an instance', () => {
    const pipe = new StructuralSubdivisionPipe();
    expect(pipe).toBeTruthy();
  });
});
